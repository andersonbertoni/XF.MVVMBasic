<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModel="clr-namespace:XF.MVVMBasic.ViewModel"
             x:Class="XF.MVVMBasic.View.AlunoView">
    <ContentPage.Content>
        <StackLayout>
            <ListView x:Name="grdAlunos">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <TableView>
                                <TableRoot>
                                    <TableSection Title="Aluno">                                        
                                        <TextCell Text="{Binding Nome}" />
                                        <TextCell Text="{Binding RM}" />
                                        <TextCell Text="{Binding Email}" />
                                    </TableSection>
                                </TableRoot>
                            </TableView>                            
                        </ViewCell>
                    </DataTemplate>                    
                </ListView.ItemTemplate>
            </ListView>
            <Button Text="Incluir Aluno" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>